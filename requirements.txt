groq==0.15.0
python-dotenv==1.0.1
# gradio 6.x requires Python 3.10+. This venv is Python 3.9, so we pin the last
# 3.9-compatible gradio and a matching huggingface_hub (newer versions removed
# HfFolder, which gradio 4.x imports). Bump these if you move to Python 3.10+.
gradio==4.44.1
huggingface_hub==0.25.2
pytest>=8.0.0
